version: '2.4'
services:
  db:
    image: mysql
    container_name: empresa
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - funcionario:/var/lib/mysql

volumes:
  funcionario:
